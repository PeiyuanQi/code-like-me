# Simulacrum Configuration

# Communication mode: interactive, headless
mode=headless

# Queue directory for headless mode
queue_dir=~/.wlm/queue

# Claude model
model=claude-opus-4-6

# Max turns per task
max_turns=50

# Permission mode: default, plan, acceptEdits, dontAsk, bypassPermissions
permission_mode=default
